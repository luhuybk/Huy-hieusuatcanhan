# Kết quả dựng — chạy "node build.js" là có lại, không cần lưu vào git
dist/
life-hub-standalone.html
*.zip

# rác của macOS
.DS_Store

# sao lưu dữ liệu cá nhân, lỡ tay để trong thư mục dự án
life-hub-*.json

# cấu hình máy chủ: chứa mã mật khẩu của bạn, không đưa lên GitHub
api/config.php
api/data/
*.sqlite
*.sqlite-*

# bản giải nén của dist (do zip bung ra), không phải mã nguồn
life-hub-dist/
